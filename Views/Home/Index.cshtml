@model HomeViewModel

<partial name="_Carousel" />

<h2>Mobiles of the week</h2>
<h4>Our weekly selection - just for you!</h4>

<div>
    @foreach (var mobile in Model.MobilesOfTheWeek)
    {
        <partial name="_MobileCard" model="mobile" />
    }
</div>